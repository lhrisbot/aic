# DeepSeek 前端开发提示词

你现在是一名资深 Web 前端工程师和 UI/UX 设计师，请帮我从 0 到 1 完成一个可运行的 Vue3 前端项目。

项目名称暂定：

**「遗韵智创 —— AI 非遗多模态内容创作平台」**

这是一个软件工程课程设计，同时后续会参加 AI 智能文化类比赛，因此项目不能只是普通后台管理系统，需要兼顾：

1. 完整的软件产品体验
2. 现代、专业、有文化特色的 UI
3. 良好的前端工程结构
4. 后续方便接 FastAPI 后端
5. 能够用于比赛演示

---

## 一、项目背景

本项目主要面向：

- 文旅宣传工作人员
- 文化馆工作人员
- 景区宣传运营人员
- 新媒体运营人员
- 非遗文化传播人员

用户在进行非遗宣传时，经常需要：

- 查询非遗资料
- 编写公众号宣传文案
- 编写短视频脚本
- 编写直播话术
- 编写文创包装文案
- 生成宣传视频

传统流程需要查资料、整理资料、重新写文案，耗时较长。

本平台希望通过：

**「可信非遗知识库 + AI 场景化内容生成 + AI 视频生成」**

帮助用户完成：

**查非遗 → 选择宣传场景 → AI 生成内容 → 编辑 → 保存作品 → 进一步生成视频**

后端未来采用 FastAPI。

AI 能力未来通过 Coze Workflow API 实现，包括：

- 非遗知识库检索
- RAG
- 场景化内容生成
- 视频脚本生成
- 分镜生成
- AI 视频生成

当前阶段只开发前端，因此所有接口先使用 Mock 数据模拟，但代码结构必须预留真实 API。

---

## 二、技术栈要求

必须使用：

- Vue 3
- Vite
- TypeScript
- Vue Router
- Pinia
- Axios
- Element Plus

建议：

- ECharts：后续数据统计
- markdown-it：渲染 AI 返回的 Markdown
- Element Plus Icons 或 Iconify

统一使用：

```vue
<script setup lang="ts">
```

不要使用：

- Vue2
- jQuery
- Bootstrap
- 老旧后台模板

项目必须能够：

```bash
npm install
npm run dev
```

直接运行。

---

## 三、整体 UI 风格

设计关键词：

**现代国风、AI、非遗、文化、高级感、简洁、专业、年轻化**

不要做成传统政府网站。

不要大量使用：

- 大红色
- 金黄色
- 龙纹
- 大量祥云
- 毛笔字体
- 复杂传统边框

不要做成廉价“古风网站”。

建议视觉风格：

- 米白色 / 浅灰白作为主要背景
- 深墨色文字
- 少量朱红色作为强调色
- 大量留白
- 大圆角卡片
- 柔和阴影
- 中国传统纹样仅作为很淡的背景装饰
- 图片尽量采用非遗文化、传统工艺类视觉
- 整体参考现代 AI SaaS 产品

导航栏采用顶部导航，不使用传统后台系统左侧菜单。

页面最大内容宽度建议：

**1200px ~ 1440px**

至少适配：

- 1920 × 1080
- 1440 × 900
- 普通笔记本电脑屏幕

---

## 四、整体导航

顶部导航栏：

左侧：

- 遗韵智创 Logo / 文字 Logo

中间：

- 首页
- 非遗探索
- AI 创作
- 视频创作
- 我的作品

右侧：

未登录：

- 登录
- 注册

已登录：

- 用户头像
- 用户名
- 个人中心
- 退出登录

当前路由需要有明显但简洁的选中状态。

---

## 五、页面一：首页

路由：

```text
/
```

首页不是后台 Dashboard，而是一个完整的产品官网首页。

### Hero 第一屏

主标题：

**让千年非遗，被今天的人看见**

副标题：

**基于可信非遗知识库的 AI 多模态文化内容创作平台**

简介：

帮助文旅工作者快速完成非遗资料查询、场景化内容创作、视频脚本与宣传视频生成。

两个按钮：

- 开始创作
- 探索非遗

右侧使用一个有设计感的 AI + 非遗视觉区域，可先使用占位图、渐变卡片或文化元素。

### 第二部分：场景能力

标题：

**一个非遗项目，多种传播方式**

展示四个场景卡片：

1. 文旅推文
2. 短视频脚本
3. 直播话术
4. 文创包装

每张卡片包含：

- 图标
- 标题
- 一句描述
- Hover 动效

### 第三部分：产品工作流

展示：

**寻找非遗 → 选择场景 → AI 智能创作 → 人工优化 → 视频生成**

要求有较好的视觉流程表现。

### 第四部分：核心技术特色

三个核心能力：

#### 可信知识

基于非遗知识库进行检索增强生成，降低事实错误。

#### 场景化创作

针对公众号、直播、短视频、文创等场景进行针对性生成。

#### 多模态生成

从文化知识到文案、脚本、分镜和视频的一体化创作。

### 第五部分：热门非遗推荐

展示：

- 皮影戏
- 昆曲
- 苏绣
- 剪纸
- 景泰蓝
- 川剧变脸

用卡片展示，点击进入非遗详情。

---

## 六、页面二：非遗探索

路由：

```text
/heritage
```

页面标题：

**探索中国非遗**

副标题：

**发现传统文化背后的故事，并使用 AI 开启新的传播方式。**

顶部提供大搜索框：

```text
请输入非遗名称，例如：皮影戏
```

分类筛选：

- 全部
- 传统戏剧
- 传统美术
- 传统技艺
- 传统音乐
- 传统舞蹈
- 民俗

地区筛选：

- 全部
- 北京
- 江苏
- 浙江
- 四川
- 陕西
- 广东
- 其他

下面使用响应式卡片布局。

每张非遗卡片包含：

- 封面图片
- 非遗名称
- 所属地区
- 所属类别
- 一句简介
- 标签

Mock 数据至少准备 8~12 个非遗项目。

---

## 七、页面三：非遗详情

路由：

```text
/heritage/:id
```

以皮影戏为例。

顶部展示：

- 大图
- 皮影戏
- 陕西
- 传统戏剧
- 相关标签

主要内容：

- 非遗简介
- 历史渊源
- 艺术特色
- 制作 / 表演技艺
- 文化价值
- 相关故事
- 参考资料

参考资料区域需要专门设计 `SourceCard`。

正文底部必须有明显 CTA：

**使用该非遗进行 AI 创作**

点击跳转：

```text
/creation?heritageId=xxx
```

---

## 八、页面四：AI 创作页

这是整个系统最重要的页面。

路由：

```text
/creation
```

不要设计成 ChatGPT 聊天页面。

采用：

**左侧创作参数 + 中间 AI 生成结果 + 右侧参考知识**

三栏布局。

建议比例：

- 左侧：260~300px
- 中间：自适应
- 右侧：280~320px

### 左侧：创作设置

非遗项目：

- 可搜索下拉框

创作场景：

- 文旅推文
- 短视频
- 直播话术
- 文创包装

不同场景动态显示不同参数。

### 文旅推文参数

传播平台：

- 公众号
- 小红书
- 微博

目标人群：

- 年轻游客
- 家庭游客
- 文化爱好者

内容风格：

- 专业文化
- 国潮年轻
- 故事化
- 轻松活泼

内容长度：

- 300 字
- 500 字
- 800 字

重点内容：

- 用户自定义输入

### 短视频参数

发布平台：

- 抖音
- 视频号
- B站

视频时长：

- 15 秒
- 30 秒
- 60 秒

视频风格：

- 国潮
- 纪录片
- 年轻化
- 故事感

传播目标：

- 文化科普
- 旅游引流
- 品牌宣传

### 直播话术参数

直播平台：

- 抖音
- 视频号
- 淘宝直播

直播场景：

- 非遗介绍
- 文创带货
- 景区宣传

语气：

- 专业
- 热情
- 年轻

时长：

- 1 分钟
- 3 分钟
- 5 分钟

### 文创包装参数

显示：

- 产品类型
- 包装风格
- 目标人群
- 宣传重点

左侧底部放主要按钮：

**AI 开始创作**

### 中间：AI 生成结果

初始状态显示 Empty State：

> 选择一个非遗项目和创作场景，开始你的 AI 非遗创作。

生成时：

- Loading
- Skeleton

Mock 模拟 1~2 秒返回。

生成完成展示：

- 标题
- 正文
- Markdown 渲染

顶部显示：

> AI 已基于 X 条非遗资料完成创作

操作：

- 重新生成
- 编辑
- 复制
- 保存作品

如果当前场景为短视频：

额外显示：

- 生成视频脚本
- 进入视频创作

编辑模式允许用户修改 AI 返回内容。

### 右侧：参考知识

标题：

**AI 参考资料**

显示：

- 当前非遗名称
- 检索到的知识数量

每条资料显示：

- 资料标题
- 资料片段
- 资料来源
- 相关度，例如 95%

右侧需要明显体现：

> 本次生成基于知识库中的可信资料，而不是完全自由生成。

---

## 九、页面五：视频创作

路由：

```text
/video
```

顶部显示：

**皮影戏 · 国潮非遗宣传短片**

使用 Step 步骤条：

1. 宣传文案
2. 视频脚本
3. 分镜设计
4. AI 视频生成

展示：

- 视频标题
- 视频时长
- 目标平台
- 视频风格

下方使用 `StoryboardCard` 展示分镜。

每个分镜包含：

- 镜头编号
- 时间
- 画面描述
- 旁白
- AI Video Prompt
- 编辑按钮
- 删除按钮
- 重新生成按钮

示例：

```text
镜头 01
0-5 秒

画面：
昏暗的幕布逐渐亮起，一道人物剪影出现。

旁白：
一灯、一幕、一双手，讲述跨越千年的故事。
```

底部：

**根据当前分镜生成视频**

模拟视频状态：

```text
pending → generating → success
```

需要展示：

- 等待生成
- 生成中
- 生成成功
- 生成失败

生成中使用 Progress / Loading。

生成完成：

- Video Player 占位区域
- 保存作品
- 重新生成
- 下载视频

---

## 十、页面六：我的作品

路由：

```text
/works
```

标题：

**我的作品**

顶部筛选：

- 全部
- 文旅推文
- 短视频
- 直播话术
- 文创包装

提供：

- 关键词搜索
- 时间排序

作品采用 Grid 卡片。

普通文章作品：

- 非遗名称
- 作品类型
- 作品标题
- 内容摘要
- 创建时间
- 查看
- 编辑
- 删除

视频作品：

- 视频封面
- 播放按钮
- 非遗名称
- 视频时长
- 视频风格
- 播放
- 查看脚本
- 删除

作品详情路由：

```text
/works/:id
```

---

## 十一、登录注册

路由：

```text
/login
/register
```

现代简洁设计。

左侧可以做文化视觉 Banner，右侧登录卡片。

登录字段：

- 用户名
- 密码

注册字段：

- 用户名
- 邮箱
- 密码
- 确认密码

当前使用 Mock 登录。

登录成功：

- Pinia 保存用户信息
- localStorage 模拟 Token

---

## 十二、项目目录

建议结构：

```text
src/
├── api/
│   ├── auth.ts
│   ├── heritage.ts
│   ├── ai.ts
│   ├── video.ts
│   └── work.ts
│
├── assets/
│
├── components/
│   ├── AppHeader.vue
│   ├── AppFooter.vue
│   ├── HeritageCard.vue
│   ├── WorkCard.vue
│   ├── SourceCard.vue
│   ├── SceneSelector.vue
│   ├── GenerationResult.vue
│   └── StoryboardCard.vue
│
├── layouts/
│   └── MainLayout.vue
│
├── views/
│   ├── Home/
│   ├── Heritage/
│   ├── Creation/
│   ├── Video/
│   ├── Works/
│   ├── Login/
│   └── Register/
│
├── router/
│   └── index.ts
│
├── stores/
│   ├── user.ts
│   └── creation.ts
│
├── types/
│   ├── heritage.ts
│   ├── ai.ts
│   ├── work.ts
│   └── video.ts
│
├── utils/
│   └── request.ts
│
├── mock/
│   ├── heritage.ts
│   ├── works.ts
│   └── ai.ts
│
├── App.vue
└── main.ts
```

---

## 十三、API 设计预留

所有 Mock 调用都通过 api 层，不要直接在 Vue 页面里写死。

未来需要接：

```text
POST   /api/auth/login
POST   /api/auth/register

GET    /api/heritages
GET    /api/heritages/:id

POST   /api/ai/generate

POST   /api/video/generate
GET    /api/video/:id/status

GET    /api/works
GET    /api/works/:id
POST   /api/works
PUT    /api/works/:id
DELETE /api/works/:id

POST   /api/feedback
```

封装统一 `request.ts`，包括：

- baseURL
- timeout
- request interceptor
- response interceptor

未来 Token：

```http
Authorization: Bearer xxx
```

---

## 十四、TypeScript 类型

必须定义以下 interface：

### Heritage

字段：

- id
- name
- category
- region
- cover
- summary
- history
- features
- technique
- cultureValue
- sources
- tags

### CreationRequest

字段：

- heritageId
- scene
- platform
- audience
- style
- length
- focus

### CreationResult

字段：

- id
- title
- content
- sources
- createdAt

### Source

字段：

- id
- title
- content
- source
- similarity

还需要：

- Work
- VideoTask
- Storyboard

---

## 十五、Mock 数据

当前后端还没有完成，请提供完整 Mock 数据。

至少提供：

- 皮影戏
- 昆曲
- 苏绣
- 剪纸
- 景泰蓝
- 川剧变脸
- 古琴
- 龙泉青瓷
- 苗族银饰
- 木版年画

每个项目包含：

- 名称
- 地区
- 类型
- 简介
- 历史
- 特色
- 文化价值
- 参考资料

AI 创作接口：

模拟等待 1~2 秒，然后返回一段高质量非遗宣传文案以及 2~3 个来源。

视频生成模拟：

```text
pending
→
generating
→
success
```

---

## 十六、交互要求

加入合理的：

- Hover 动效
- Transition
- Skeleton
- Loading
- Empty State
- Toast
- Dialog
- Confirm

删除作品必须二次确认。

保存成功：

```ts
ElMessage.success(...)
```

API 失败：

```ts
ElMessage.error(...)
```

避免过度动画。

---

## 十七、代码要求

代码必须：

- 结构清晰
- 组件化
- 可维护
- 可扩展
- 使用 TypeScript
- 合理拆分组件

不要：

- 把所有代码写在 App.vue
- 单个页面写 1000 行以上
- 在页面中到处直接写 API 请求

公共组件一定拆分。

CSS 可以使用：

- Scoped CSS
- SCSS

统一维护：

- 颜色
- 字体大小
- 间距
- 圆角

建议使用 CSS Variables：

```css
--primary-color
--text-primary
--text-secondary
--background-color
--border-radius
```

---

## 十八、比赛展示要求

项目未来会参加 AI 智能文化类比赛。

UI 需要重点突出：

**AI + 非遗 + 可信知识 + 多模态创作**

尤其在：

- 首页
- AI 创作页
- 视频创作页

要让评委在几十秒内理解：

- 系统解决什么问题
- 和普通 AI 对话工具有什么区别
- 为什么需要可信知识库
- 为什么适合文旅内容生产

页面中不要出现：

- 学校名称
- 学校 Logo
- 指导教师信息

---

## 十九、开发方式

不要一次直接输出所有项目代码。

请按照以下阶段开发。

### 第一阶段：架构设计

先给出：

1. 完整页面结构
2. Vue Router 路由结构
3. 组件拆分
4. 数据模型设计
5. API 设计
6. 项目目录
7. UI 风格规范

等我确认之后再开始写代码。

### 第二阶段：基础工程

完成：

- Vite
- Vue3
- TypeScript
- Element Plus
- Vue Router
- Pinia
- Axios
- MainLayout
- Header
- Footer
- Router
- 全局样式

### 第三阶段：实现首页

### 第四阶段：实现非遗探索和非遗详情

### 第五阶段：实现 AI 创作页

这是最高优先级页面，需要重点打磨。

### 第六阶段：实现视频创作页

### 第七阶段：实现我的作品

### 第八阶段：实现登录注册

### 第九阶段：整理 Mock API 和 Axios API 层

### 第十阶段：全项目检查

检查：

- TypeScript 错误
- 路由错误
- UI 一致性
- 响应式
- 组件复用
- 运行错误

每完成一个阶段都告诉我：

- 新增了哪些文件
- 修改了哪些文件
- 当前已经实现什么
- 下一阶段准备做什么

**不要擅自改变产品定位。**
